db:
  host: ${DB_HOST}
  # port: ${DB_PORT}
  port: 5432 # need to find a way to handle this since serde_aux won't parse it because it to u16 as it is an env variable
  username: ${DB_USERNAME}
  password: ${DB_PASSWORD}
  database_name: ${DB_NAME}
  require_ssl: true
app:
  port: ${APP_PORT}
  # base_url: ${BASE_URL}
  env: ${APP_ENV}
  m_cost: ${M_COST}
  p_cost: ${P_COST}
  t_cost: ${T_COST}
  jwt_secret: ${JWT_SECRET}
email:
  smtp_user: ${SMTP_USER}
  smtp_pass: ${SMTP_PASS}
  smtp_server: ${SMTP_SERVER}
